---
name: aiag-vda-spc-assistant
version: 3.0.0
description: 基于 AIAG-VDA SPC 第三版 / ISO 22514 的新版统计过程控制（SPC）分析助手。使用精确分布分位数（w分布/χ²分布/二项分布/泊松分布）计算控制限，覆盖能力指数四族（Cp/Cpk/Pp/Ppk/Pm/Pmk/Cw/Cwk + 通用几何法分位数），Nelson 1-8 判异，Anderson-Darling 正态性检验，时间相关分布模型 A/B/C/D 判定，机器性能放行。用于新版 SPC 分析、精确控制图、过程能力研究、设备放行。
dependency:
  python:
    - numpy>=1.21.0
    - scipy>=1.7.0
---

# 新版 SPC 分析（AIAG-VDA 第三版 / ISO 22514）

## ⚠️ 内置原文范围声明（防幻觉）

本技能引用了下列标准/手册，但 `references/01-标准原文/` 中**未内置其全文原文**：

- AIAG-VDA SPC 手册（第 2 版）全文 — AIAG 付费出版物，需用户提供
- PPAP 手册（第 4 版）全文 — AIAG 付费出版物，需用户提供

**因此，涉及上述标准时必须遵守：**

1. **先查，再答。** 引用具体条款号前，必须先执行
   `python scripts/clause_lookup.py --clause <条款号>` 取回原文。
   脚本返回「条款不存在」即代表原文未内置。
2. **严禁凭记忆复述。** 不得用模型知识「还原」上述标准的条款内容，
   不得用相近条款顶替，也不得跨标准套用
   （例如拿 ISO 9001 的 `8.5.1` 冒充 AS9100D 的 `8.5.1`）。
   「只给条款号、不给原文」正是幻觉的直接来源。
3. **诚实降级。** 正确做法是如实告知用户：
   「该标准原文未内置，无法提供条款级依据，请将标准 PDF 放入
   `references/01-标准原文/` 后重试。」——然后停止该部分的判定，
   改由用户对照标准原文确认。

## 任务目标

- 本 Skill 用于：新版统计过程控制分析——**精确分布控制限**控制图、能力指数**四族**（Cp/Cpk/Pp/Ppk/Pm/Pmk/Cw/Cwk + 通用几何法分位数）、Nelson 1-8 判异、Anderson-Darling 正态性检验、时间相关分布模型 A/B/C/D 判定、机器性能（设备放行）研究、非正态 z-分数法。
- 触发条件：用户请求"新版 SPC 分析、精确控制图、过程能力四族、Pm/Pmk 机器性能、Cw/Cwk、时间模型、ISO 22514 能力研究"，或明确要求"按 AIAG-VDA 第三版 / 不用 A2/D4 近似"。

## 核心概念（新版 vs 旧版根本差异）

| 维度 | 旧版（AIAG SPC 第二版） | 新版（AIAG-VDA 第三版 / ISO 22514） |
|------|------------------------|--------------------------------------|
| 能力指数 | Cp/Cpk（within）、Pp/Ppk（overall） | **四族**：Cp/Cpk、Pp/Ppk、**Pm/Pmk**、**Cw/Cwk** + 通用几何法（分位数） |
| 控制限 | A2/D3/D4 等经验常数（正态近似） | **精确分布**：w 分布（R）、χ²（s）、二项（p/np）、泊松（c/u） |
| 分布假设 | 默认正态 | 明确区分；新增 z-分数法 / 超标比例法处理非正态 |
| 时间维度 | 基本未涉及 | 新增 ISO 22514-2 时间模型 A/B/C/D |
| 机器性能 | 无 | 第 7 章 Pm/Pmk（生产设备放行） |
| 计数型 | 3σ 正态近似 | **明确建议避免正态近似**，用软件算精确分位数 |
| 组内能力 | 无单独概念 | 新增 Cw/Cwk，**仅诊断用，不得用于正式报告** |

## 能力清单（本技能覆盖）

1. **计量值控制图（精确分布限）**：X̄-R（w 分布）、X̄-s（χ² 分布）、I-MR（n=1）；控制限用精确分位数（α=0.0027，即 ±3σ 等价），非传统常数。
2. **计数值控制图（精确分位数限）**：p / np（二项 BINOM.INV）、u / c（泊松 CHISQ.INV）；支持变样本量阶梯限；判异规则 1 判精确限越界、规则 2-4 用标准化值。
3. **能力指数四族**：Cp/Cpk、Pp/Ppk、Pm/Pmk、Cw/Cwk；正态用经典式，非正态用**通用几何法（分位数法，不假设分布）** 与 **z-分数法 / 超标比例法**。
4. **正态性检验**：Anderson-Darling（主），p<0.05 拒绝正态 → 触发 Form B / 非正态路径。
5. **判异**：Nelson 1-8（默认全开，可单独关闭）。
6. **时间相关分布模型 A/B/C/D**：按位置/变差是否随时间恒定分类，决定散布估计策略。
7. **机器性能 Pm/Pmk**：第 7 章设备放行；含减样本量时的 χ² 目标值修正。
8. **稳态硬规则**：Nelson 任意规则触发 → 判不稳态 → Cp/Cpk/Cpm/Cpmk 输出 **NA**（仅 Pp/Ppk 可报）。
9. **报告模板**：Form A（正态+稳定）/ Form B（非正态+扩展休哈特）/ Form C（图纸对照）三形态，21 要素。

## 操作步骤

### 步骤1：交互收集协议（强制交互锁）

本技能采用**引导式收集**：逐项向用户索取真实信息，绝不代写、绝不凭经验默认。按以下锁顺序执行，任一硬锁未满足不得进入分析。

**字段三态分类（对应报告要素 1–11 抬头）**

| 要素 | 字段 | 收集态 | 规则 |
|------|------|--------|------|
| 1 | 过程名 | **必填** | 哪条产线/工序 |
| 2 | 零件名 | **必填** | 什么零件 |
| 3 | 特性名 | **必填** | 测什么尺寸/参数 |
| 4 | 规格限 LSL/USL | **条件必填** | 双侧必给；单侧只给一边；无规格→能力块整块 NA（不填 0 默认） |
| 5 | 日期 | 自动带出 | 默认今天，用户可改 |
| 6 | 客户/项目号 | 选填 | 对外报告追溯用 |
| 7 | 数据来源 | 选填 | 谁测/哪个系统导出 |
| 8 | 子组信息 k/n | 自动带出 | 引擎解析后回显确认 |
| 9 | 测量系统描述 | 选填 | 量具/分辨力/重复性，仅描述不计算 |
| 10 | 技术条件 | 选填 | 公差等级/客户特殊要求 |
| 11 | 偏差说明 | 选填 | 已知异常/换刀/换班 |
| — | 图纸值 | **条件必填** | 仅 Form C 触发时收 |

> Target 不单独收集：自动 `(LSL+USL)/2` 带出，用户可改。

**收集流程（带锁）**

1. **[锁1] 身份三件套**：依次问 过程名 / 零件名 / 特性名。缺任一不开分析。
2. **[锁2] 数据**：引导上传/粘贴（计量值矩阵或单列、计数值 1/2 列）；调引擎解析 → 回显 k、n、正态性初判、建议 Form，请用户确认。
3. **[锁3] 规格限**：三选一——双侧（LSL+USL）/ 单侧（只给一边）/ 无规格。决定能力块是否 NA。
4. **[条件锁] 图纸值**：仅当建议 Form C 时出现，追问图纸对照值。
5. **[选填批]**：客户/项目号、技术条件、偏差说明、数据来源、测量系统描述——一次性列出，允许整体跳过。

**防错**：每收完一组锁，将已确认信息**回显一遍**供核对，再进下一步；N%n≠0 立即红条报错并禁用分析（尾部残点不静默丢弃）；至少需合理子组数（建议 k≥20，机器性能建议 N≥50）。

### 步骤2：调用计算引擎
- 将用户数据整理为临时 CSV/JSON，调用 `scripts/spc_v2_engine.py`：
  ```bash
  python scripts/spc_v2_engine.py --data-file ./temp.csv --usl 100 --lsl 90 --format json
  ```
- 引擎返回 JSON：基础统计量、精确控制限（X̄/R 或 s 或 I-MR，或 p/np/c/u）、Nelson 判异、Anderson-Darling、时间模型、能力四族、稳态判定、Form 选择、DPMO/sigma（计数值）。
- 本技能**完全自包含**：引擎产出 JSON 后，由智能体按报告模板（21 要素）直接生成结构化报告，不依赖任何外部文件或工具。

### 步骤3：结果解读与报告生成
- 智能体按引擎输出 + Form 选择，生成结构化报告（模板见下）。
- **稳态硬规则**：不稳态时 Cp/Cpk/Cpm/Cpmk 显示 NA，并提示"先消除特殊原因，稳定后再评能力"。
- **MSA 仅描述性引用**：报告中"测量过程"段写一段文字（分辨力/准确度/重复性再现性/不确定度），不现场算 GRR；若用户需要量化，引导使用独立的 MSA 分析工具。
- **能力卡四色**：pass（绿）/fail（红）/warn（黄）/na（灰），阈值参照行业（汽车 Cpk≥1.33、PPAP Ppk≥1.67）。

### 步骤4：异常处理
- N%n≠0：报错，禁分析。
- 数据量不足：提示补充（机器性能建议 N≥50）。
- 非正态：走 Form B / 通用几何法 / z-分数法，不强行正态路径。
- 单侧规格：仅算对应侧指数；存在自然极限时 Cw/Cwk 作诊断。

## 数据格式契约

- **计量值矩阵**（推荐）：每行一个子组，每列一次测量；首行若为表头（非数值）自动跳过；n = 列数自动识别。
- **计量值单列**：按当前 n 顺序切块；N%n≠0 报错。
- **计数值**：单列=缺陷数（n=1→c，否则→u）；两列=n,x（→p/np）；自动识别。
- **分隔符**：逗号 / 制表 / 分号 / 空格自动识别。
- 详例见 [assets/data-template.csv](assets/data-template.csv)。

## 计算引擎约定

- `scripts/spc_v2_engine.py` 为 self-contained 计算核心（numpy + scipy），不调外部服务。
- 精确控制限实现见 [references/formulas_ref.md](references/formulas_ref.md)（含 w/χ²/二项/泊松分位数方法与页码索引）。
- 完整公式溯源见 [references/formulas_ref.md](references/formulas_ref.md)（技能自包含，不依赖外部路径）。

## 报告模板（21 要素，Form A/B/C 自动判定）

- 要素 1-11 抬头（过程/零件/特性/规格/日期/客户项目/数据来源/子组信息/测量系统描述/技术条件/偏差说明）
- 要素 12-14 数据概览（N/k/n、均值、标准差、极差、正态性结论）
- 要素 15-18 图表（直方图 / 链图 / 概率图 / 控制图；Form B 用扩展休哈特图 + ±1σ/±2σ 分区；Form C 用图纸值对照表 + 能力条形图）
- 要素 19-20 统计（控制限表、Nelson 判异列表、时间模型分类）
- 要素 21 结论（稳态判定 + 能力四族卡 + 改进建议 + 免责声明）

## 注意事项

- **稳态硬规则不可破**：不稳态时 Cp/Cpk 一律 NA，不得"算出来好看"。
- **MSA 不现场算**：仅描述性引用，量化交给独立的 MSA 分析工具。
- **Cw/Cwk 仅诊断**：不得用于正式能力报告。
- **计数型避免正态近似**：必须用二项/泊松精确分位数。
- **精确 vs 近似**：X̄-R 的 R 图用 w 分布精确限；若需与传统 A2/D4 对照，见 references。
- **跨平台**：逻辑与计算解耦，扣子/WorkBuddy/SkillHub 通用；依赖仅 numpy/scipy。

## 验收（开发期铁律）

- 引擎须有自测：`tests/test_engine.py` 用技能内置样本数据（tests 内联，不依赖外部文件）跑端到端，关键数值（Cp/Cpk/Pp/Ppk、控制限、Nelson 触发、AD 结论、图表类型）合理且与 scipy/已知值对照。
- 不迷信单测：须浏览器/命令行端到端跑通整条链路（数据→引擎→报告）。

## 使用示例

### 示例1：新版 X̄-R 精确控制限 + 能力四族
```
用户：按新版 AIAG-VDA 帮我分析这组数据，USL=100，LSL=90，25 子组每行 5 个
流程：
1. 解析矩阵（k=25, n=5）
2. python scripts/spc_v2_engine.py --data-file data.csv --usl 100 --lsl 90 --format json
3. 输出：w 分布精确 R 限、A2 等价 X̄ 限、Cp/Cpk/Pp/Ppk/Pm/Pmk/Cw/Cwk、Nelson、AD、Form A
```

### 示例2：机器性能放行（Pm/Pmk）
```
用户：新设备验收，50 件连续加工，USL/LSL，按第三版第 7 章算 Pm/Pmk
流程：引擎 --mode machine --n-sample 50，输出 Pm/Pmk + 目标值（按特性重要度）+ 减样本量修正
```

### 示例3：计数值 p 图（精确限）
```
用户：20 批每批 50 件的不合格数，画 p 图
流程：引擎识别 2 列 n,x → 二项精确分位数控制限（阶梯，变 n），规则 1 判越界
```

## 反馈与问题咨询
- 本技能的使用反馈、问题咨询、改进建议，请发邮件至：**engicool@agent.qq.com**
- 该邮箱仅用于本技能的使用反馈与问题咨询，不承接营销或其他无关诉求。
